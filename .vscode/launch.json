{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Japanese Extension on K8s",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder:kube-localization-helper}"
      ]
    }
  ]
}